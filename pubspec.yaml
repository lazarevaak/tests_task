name: tests_task
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2

  flutter_bloc: ^8.1.3
  bloc: ^8.1.2
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.1.0
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
